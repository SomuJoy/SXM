{
    "$schema": "https://json-schema.org/schema",
    "cli": "nx",
    "$id": "service-cms-endpoint",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Name of the cms data service",
            "x-prompt": "Name of the cms data service?"
        },
        "project": {
            "type": "string",
            "description": "project name",
            "x-prompt": "Name of the project (lib)?"
        },
        "getDataMethodName": {
            "type": "string",
            "description": "Name of the method to get data",
            "x-prompt": "Name of the method to get data?"
        }
    },
    "required": ["name", "project"]
}
