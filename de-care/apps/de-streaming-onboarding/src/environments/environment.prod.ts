export const environment = {
    production: true,
    settings: {}, // empty object for prod settings because these come from settings.js file to support deployment configs
    streamingOnboardingSettings: {} // empty object for prod settings because these come from settings.js file to support deployment configs
};
