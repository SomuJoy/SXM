export enum verificationResponse {
    'success' = 'success',
    'failure' = 'failure',
    'rejected' = 'rejected',
    'docReviewLimitExeeded' = 'docReviewLimitExeeded',
    'noVerification' = 'noVerification'
}

export enum passFailResponse {
    'success' = 'success',
    'failure' = 'failure'
}
